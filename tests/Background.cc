#include <gtest/gtest.h>

#include "objects/game/environment/Background.hpp"

using namespace GameEnvironment;

TEST(Background, LoadTexture) {
  // Background background = Background();
  // int existingTexture = background.setTexture("assets/background/background_0.png");

  // EXPECT_EQ(existingTexture, 1);

  // int nonExistingTexture = background.setTexture("assets/background/background_123.png");
  // EXPECT_EQ(existingTexture, 0);
}